To start the local server to show the UI, run
start-server-command.bat 
from the home location of the app (directory mockup-app)

To start the OH MSG server to send messages between the users, run 
start-ohmsg-server.bat
from the home location of the app (directory mockup-app)

